# This environment installed only the required packages for testing the
# iris ramp-kit
name: ramp-iris
channels:
  - conda-forge
dependencies:
  - numpy
  - scikit-learn
  - pandas
  - click
  - pip
  - pip:
    - ramp-workflow==0.5.0
