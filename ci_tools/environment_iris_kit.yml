# This environment installed only the required packages for testing the
# iris ramp-kit
name: ramp-iris
channels:
  - conda-forge
dependencies:
  - numpy
  - scikit-learn
  - pandas
  - click=8.0.4  # workaround for ramp-workflow#307
  - pip
  - pip:
    - ramp-workflow
